import { StyleSheet } from 'react-native';

import {
  MYMESSAGEBG,
  MYMESSAGETEXT,
  THEIRMESSAGEBG,
  THEIRMESSAGETEXT,
} from '../../masterStyle.js';

export default StyleSheet.create({

  sw: {    
    flex: 1,
    flexDirection: 'column',
    margin: 5,
    marginTop: 10,
  },
});