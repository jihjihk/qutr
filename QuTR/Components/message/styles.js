import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  message: {
    fontSize: 17.5,
    padding: 10,
    borderRadius: 15,
    overflow: 'hidden',
    margin: 7.5,    
    maxWidth: 250,
  },
});