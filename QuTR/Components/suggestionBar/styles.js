import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  withKeyboard: {
    height: 40, 
    flexDirection: 'row',
  },
  withoutKeyboard: {
  	height: 0,
    flexDirection: 'row',
  }
});